export enum UserRole {
  VISITOR = 'visitor',     // Usuario sin verificar email
  USER = 'user',           // Usuario con email verificado
  ADMIN = 'admin'          // Administrador con permisos completos
}
